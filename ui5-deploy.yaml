# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: qa_rejection_srv
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: DS4_100_HTTPS
          url: http://vhpuads4ci.sap.essa.id:44300
          client: "100"
        app:
          name: qa_rejection_srv
          package: Z001
          transport: DS4K916502
        exclude:
          - /test/
